import { setupApplication } from 'flavours/bird-ui/setup';
import { loadLocalization } from 'flavours/bird-ui/locales';

// Initialize Bird UI
setupApplication();

export * from 'flavours/vanilla/entrypoints/application';
